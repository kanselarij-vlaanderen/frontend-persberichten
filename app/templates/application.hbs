{{page-title "Persberichten"}}

<MainHeader
  @contactRoute="contact"
  @contactIcon="circle-question"
  @contactSize="default"
  @selectedMonth={{this.selectedMonth}}
  @selectOptions={{this.months}}
  @selectFunction={{this.changeMonth}}
>
  <WebComponents::AuTab @route="press-releases">persberichten</WebComponents::AuTab>
  <WebComponents::AuTab @route="mailing-lists">verzendlijsten</WebComponents::AuTab>
  <WebComponents::AuTab @route="contacts">contacten</WebComponents::AuTab>
</MainHeader>
{{outlet}}