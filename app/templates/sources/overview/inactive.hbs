{{page-title "Inactieve bronnen"}}

<Contact::Table
  @model={{this.model}}
  @sort={{this.sort}}
  @page={{this.page}}
  @size={{this.size}}
  @nextPage={{this.nextPage}}
  @prevPage={{this.prevPage}}
  @setSizeOption={{this.setSizeOption}}
/>
